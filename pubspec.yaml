name: walid
description: WALID - نظام إدارة التربية البدنية والرياضية
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  intl: ^0.20.2
  file_picker: ^10.0.0
  excel: ^4.0.0
  archive: ^3.3.0
  path_provider: ^2.0.0
  open_filex: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
